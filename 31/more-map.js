const number = [12,34,23,32];
const doubled = number.map(num => num*2);
console.log(doubled);

const fiveBonus = number.map(num => num+5)
console.log(fiveBonus)

const halves = number.map(num => num / 2 )
console.log(halves)